# Telegram Commands

Supported commands: `/start`, `/help`, `/status`, `/agents`, `/projects`, `/assign [agent] [task]`, `/tasks`, `/approvals`, `/approve [approval_id]`, `/reject [approval_id] [reason]`, `/daily`, `/weekly`.

`TELEGRAM_ALLOWED_USER_IDS` empty allows all users. A comma-separated value restricts access to those user IDs.
